import { Repository } from 'typeorm';
import { <%= modelClass %> } from './<%= modelDir %><%= modelFileName %>';
import {CustomRepository} from "../util/typeorm/typeorm-ex.decorator";

@CustomRepository(<%= modelClass %>)
export class <%= nameUpper %>Repository extends Repository<<%= modelClass %>> {

}
